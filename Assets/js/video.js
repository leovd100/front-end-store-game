export const videoObj = (pathVideo, logo, person, background) => {
  const video_path = pathVideo;
  const video_logo = logo;
  const video_person = person;
  const video_background = background;

  return {video_path, video_logo,video_background, video_person}
}